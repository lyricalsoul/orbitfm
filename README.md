# rpc integration for last.fm
- clone the repository
- run `npm i`
- rename `.env.example` to `.env` and add your last.fm api key and username
- run `npm run start`

![what the rpc will look like](.github/sample.png)